import RegisterForm from "@/components/RegisterForm/registerForm"

const register = () => {
    return (
        <main className="body flex flex-col items-center justify-between p-24 h-full">
            <RegisterForm/>
        </main>
    )
}

export default register